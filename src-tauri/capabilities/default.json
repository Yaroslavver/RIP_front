{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the electrolyte guest desktop shell",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
